apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: clawless
resources:
  - namespace.yaml
  - pvc.yaml
  - configmap.yaml
  - secret.yaml
  - deployment.yaml
  - service.yaml

# Uncomment to override the default storageClassName (microk8s-hostpath):
# patches:
#   - target:
#       kind: PersistentVolumeClaim
#       name: clawless-home
#     patch: |
#       - op: replace
#         path: /spec/storageClassName
#         value: my-storage-class
